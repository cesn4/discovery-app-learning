import { setConfiguration } from 'react-grid-system';

setConfiguration({
    containerWidths: [800, 1200, 1440, 0],
    breakpoints: [376, 800, 1200, 0],
    gutterWidth: 0,
    defaultScreenClass: 'lg',
});
